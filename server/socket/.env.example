# ===================================
# Smart Basket Socket Server - Environment Variables
# ===================================
# Copy this file to .env and fill in your values

# ─────────────────────────────────────
# Application Settings
# ─────────────────────────────────────
# Environment: development | production
# - development: disables Sentry
# - production: enables Sentry error monitoring
NODE_ENV=development

# Server port (should be different from API server)
PORT=5001

# ─────────────────────────────────────
# Authentication
# ─────────────────────────────────────
# JWT Access Secret - MUST match the API server's JWT_ACCESS_SECRET
# This is used to verify access tokens from clients
JWT_ACCESS_SECRET=your-super-secret-jwt-access-key-min-32-chars

# ─────────────────────────────────────
# Redis (Optional)
# ─────────────────────────────────────
# Required for scaling with multiple socket server instances
# Enables cross-server communication via pub/sub
# Format: redis://[[username][:password]@][host][:port][/db-number]
# REDIS_URL=redis://localhost:6379

# ─────────────────────────────────────
# CORS
# ─────────────────────────────────────
# Allowed origins (comma-separated for multiple)
# Should match the API server's CORS_ORIGIN
CORS_ORIGIN=http://localhost:5173

# ─────────────────────────────────────
# API Server Connection
# ─────────────────────────────────────
# Socket server calls API to persist notifications to MongoDB
# Local: http://localhost:5000/api
# Dev: https://smartbasket-api-dev.onrender.com/api
# Prod: https://smart-basket-api-prod.onrender.com/api
API_URL=http://localhost:5000/api

# ─────────────────────────────────────
# Error Monitoring (Optional)
# ─────────────────────────────────────
# Sentry DSN - use the SAME DSN as API server
# Socket errors appear with serverName: "socket-server"
# Only sends errors when NODE_ENV=production
# SENTRY_DSN=https://xxxxx@oxxxxx.ingest.sentry.io/xxxxx
